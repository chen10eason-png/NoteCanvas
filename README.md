NoteCanvas v5.4.0: per-notebook three-dot menu (rename, favorites, duplicate, JSON backup, delete with confirmation); touch-friendly placement; dynamic centralized version label; library empty state; no changes to Pencil/eraser logic. Backup import is not yet implemented. iPad hardware test pending.
