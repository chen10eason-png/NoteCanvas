NoteCanvas V5.10: Settings > Swipe to change pages: Off (default), Horizontal, Vertical. Single-finger only, threshold 75px, direction ratio 1.35, gesture under 1.5 seconds. Pencil and multi-touch excluded. Left/up = next, right/down = previous. Preference persists in localStorage. iPad Safari hardware test still needed.
